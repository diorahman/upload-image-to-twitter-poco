#ifndef OAUTH_TYPES_H
#define OAUTH_TYPES_H

#include <map>
#include <string>

typedef std::map<std::string, std::string> Params;
typedef std::map<std::string, std::string>::iterator ParamsIterator;

#endif // OAUTH_TYPES_H
